# Anthropic API key — get from console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Zoho SMTP — use an App Password, not your account password
# Settings > Security > App Passwords in Zoho Mail
ZOHO_EMAIL=josh@forrestanalytics.com
ZOHO_APP_PASSWORD=your-zoho-app-password

# Optional: set PORT for Railway/Render (they inject this automatically)
# PORT=8000
